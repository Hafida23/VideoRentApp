@model VideoRentApp.Models.RentMovie

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RentMovie</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RentMovieId" class="control-label"></label>
                <input asp-for="RentMovieId" class="form-control" />
                <span asp-validation-for="RentMovieId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="MovieId" />
            <input type="hidden" asp-for="RenterId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
